import { PDFContextState } from "../types"
/**
 * Hook to access the PDF registry.
 * @returns The PDF registry or null during initialization
 */
export declare function useRegistry(): PDFContextState
